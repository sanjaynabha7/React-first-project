import React, {useState} from 'react'

function Pagination(showPerPage) {
 const [counter, setCounter] = useState()
 


    console.log(counter);
    return (
        <div>
            {/* <button onClick={prePost}>Previous </button>
            <button>Next </button> */}
        </div>
    )
}

export default Pagination
